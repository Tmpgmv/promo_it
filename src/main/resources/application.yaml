spring:
  application:
    name: promo_it
    debug: true
  datasource:
    url: jdbc:postgresql://localhost:5432/promo_it
    username: promo_it
    password: awerdWdgy!24
    driver-class-name: org.postgresql.Driver
  # Optional: JDBC / logging tuning
  jdbc:
    template:
      query-timeout: 30

logging:
  level:
    org.springframework.jdbc: INFO
    gift.academic: INFO

  file:
    name: /home/michael/Documents/promo_it/src/main/java/gift/academic/promo_it/logs/promo_it.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 7