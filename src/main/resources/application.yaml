spring:
  application:
    name: promo_it
    debug: true
  datasource:
    url: jdbc:postgresql://localhost:5432/promo_it
    username: promo_it
    password: awerdWdgy!24
    driver-class-name: org.postgresql.Driver
  # Optional: JDBC / logging tuning
  jdbc:
    template:
      query-timeout: 30

logging:
  level:
    org.springframework.jdbc: DEBUG